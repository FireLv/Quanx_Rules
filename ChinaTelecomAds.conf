# 中国电信 去开屏广告（QX 重写规则）
# 生成：2026-09-20 依据 quantumult-x-2026-09-19-133652.har 分析
#
# 【中国电信(电信营业厅) CTPocket 13.4.0】
# 开屏配置接口：multiactiveiosclient.json（整包加密 hex，驱动开屏+弹窗活动投放）
#   拉取时刻 15:09:52（北京），晚于开屏展示——即本次拉取驱动下次启动的开屏。
#   拒绝该配置后 App 取不到投放数据，后续启动不再出开屏（素材缓存失效后自然消亡）。
^https?:\/\/appupdates\.189\.cn\/actives\/multiactiveiosclient\.json url reject-200

# 【hostname 追加到 [mitm]（该行本身无 %APPEND%，需手工并入）】
#   appupdates.189.cn
